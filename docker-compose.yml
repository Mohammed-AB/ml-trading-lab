services:
  scalp-bot:
    build: .
    container_name: scalp-mode
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    stdin_open: true
    tty: true
